#SNAKE GAME USING CPP ON LINUX

Since linux does not have conio library you can either use ncurses(Will make the code lenghtier)

Or you can just write separate functions for using _kbhit() and _getch()

compile with:

g++ main.cpp

then to get the output, run:

./your_output_file